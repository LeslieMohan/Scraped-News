<div>
    <h1 class="card z-depth-5 center-align">Extra! Extra! <br> Read All About It!</h1>
    <div class="row">
      <div class="col s3"></div>
    </div>
</div>

<div class="row">
  <div class="col s2"></div>
  <div class ="col s8 center-align">
    {{#each article}}
      <div class="card-panel">
        <h4>{{this.title}}</h4>
        {{!-- goes to article.handlebars --}}
        <a href = "/readArticle/{{this._id}}" class="btn" id="btnSignUp">Read Article</a>
      </div>
    {{/each}}
  </div>
</div>